{
  "name": "@o-okul/worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "pnpm build && node dist/main.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/main.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "exceljs": "^4.4.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.7"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1056.0",
    "@sentry/node": "10.57.0",
    "@o-okul/db": "workspace:*",
    "@o-okul/shared-types": "workspace:*",
    "@o-okul/sms-adapter": "workspace:*",
    "bullmq": "^5.77.6",
    "pino": "10.3.1",
    "puppeteer-core": "25.1.0"
  }
}
