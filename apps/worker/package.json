{
  "name": "@uzman-hocam/worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "pnpm build && node dist/main.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/main.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.7"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1056.0",
    "@uzman-hocam/db": "workspace:*",
    "@uzman-hocam/shared-types": "workspace:*",
    "@uzman-hocam/sms-adapter": "workspace:*",
    "bullmq": "^5.77.6"
  }
}
