# Faz D Rol Portalları Checklist'i

Bu dosya `MASTER_PLAN.md` §10.6 Faz D kapsamını doğrulanabilir parçalara böler.

## Varsayımlar

- Faz C backend yüzeyleri korunur; rol portalları mevcut `/me/**` endpointlerini kullanır.
- Portal ekranları kişi hesabı ister; tenant admin oturumu rol portalı yerine kurum panelinde kalır.
- Kimlik-bağı davet/göç akışı ürün onayı beklediği için bu turda yeni davet sistemi eklenmez.

## Durum Denetimi

| Parça | Mevcut durum | Kanıt |
|---|---|---|
| `/me/**` envanteri | Başladı: öğrenci profil/devamsızlık/not, veli bağlı öğrenci profil/devamsızlık/not/ödeme, öğretmen profil/program yüzeyleri rol portallarında kullanılıyor | `pnpm --filter @uzman-hocam/web next:e2e` |
| Öğrenci portalı | Tamamlandı: Next `/ogrenci` profilde okunur sınıf ve sorumlu öğretmen ilişkisini, veli ilişkileri panelinde veli adı/ilişki tipi/izin özetini, sınıf ve kayıt geçmişini, hedefli duyuru, okunur devamsızlık özeti/listesi, öğretmen notları, okunur materyal adı ve ders/dönem bağlamlı ödev/materyal, destek talebi, gelişim grafiği ve ders/dönem bağlamlı son sınav raporunu gerçek `/me/student/**`, `/courses` ve `/academic-terms` çağrılarıyla gösterir | `pnpm --filter @uzman-hocam/api exec vitest run src/report/report-generation.service.test.ts src/report/report-generation.controller.e2e.test.ts src/school/school.e2e.test.ts src/app.e2e.test.ts`; `pnpm --filter @uzman-hocam/web typecheck`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next rol portalları bağlı kişi verisini gösterir"` |
| Öğretmen portalı | Genişledi: Next `/ogretmen` profil özetinde branş, ders/dönem, okunur sınıf ve kampüs/seviye/şube kapsamı, sorumlu öğrenci ve program sayısını; ayrıca hedefli duyuru, bugünkü dersler, sınıf bilgili ders programı, sınıf bilgili öğrenci seçimleri, ders/dönem seçimli yoklama kaydı, ders/dönem seçimli öğretmen notu girişi, ödev kontrolü, ders/dönem seçimli materyal atama, seçili öğrencinin sınıf/kayıt geçmişi, kendi destek talepleri, ders/dönem bağlamlı seçili öğrenci raporu ve kapsamındaki sınıf raporunu gerçek `/me/teacher/**`, `/students`, `/classes`, `/campuses`, `/grade-levels`, `/courses`, `/academic-terms`, `/attendance`, `/teacher-notes`, `/homework`, `/me/teacher/support-tickets` ve rapor çağrılarıyla gösterir | `pnpm --filter @uzman-hocam/api exec vitest run src/support-ticket/support-ticket.e2e.test.ts`; `pnpm --filter @uzman-hocam/web typecheck`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next rol portalları bağlı kişi verisini gösterir"` |
| Veli portalı | Tamamlandı: Next `/veli` bağlı öğrenci seçimi, profilde okunur sınıf ve sorumlu öğretmen ilişkisi, hedefli duyuru, okunur devamsızlık, öğretmen notu, ödeme planı ve okunur taksit özeti, finans izni kapalıysa ödeme görünümünü gizleme, bildirim tercihleri, destek talebi, ödev/materyal, gelişim grafiği ve ders/dönem bağlamlı son sınav raporunu gerçek `/me/guardian/**`, `/courses` ve `/academic-terms` çağrılarıyla gösterir | `pnpm --filter @uzman-hocam/api exec vitest run src/report/report-generation.service.test.ts src/report/report-generation.controller.e2e.test.ts src/school/school.e2e.test.ts src/app.e2e.test.ts`; `pnpm --filter @uzman-hocam/web typecheck`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next rol portalları bağlı kişi verisini gösterir"` |
| Rol bazlı web menüsü | Tamamlandı: kurum admin yalnız kurum menüsünü, öğretmen/öğrenci/veli yalnız kendi portal menüsünü görür; yanlış başlangıç rotası role uygun ana ekrana yönlenir | `pnpm --filter @uzman-hocam/web test:e2e` |
| Kurum rol önizleme ekranı | Tamamlandı: `/kurum/rol-onizleme` kurum admin için öğretmen/öğrenci/veli portal kapsamını, kişi hesabı şartını, erişim kurallarını ve kanıt komutlarını gösterir; admin portalları normal menü rotası olarak görmez | `pnpm --filter @uzman-hocam/web typecheck`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next login gerçek auth store ile kurum paneline geçer"` |
| Kurum menü hiyerarşisi | Tamamlandı: kurum menüsü Kişiler, Akademik, Sınav/Rapor, Finans, İletişim ve Operasyon gruplarına ayrıldı; Operasyon altında kullanıcı, rol önizleme, denetim, KVKK, güvenlik denetimi, gözlemlenebilirlik, UAT/rollback, canlı yayın, sistem sağlığı ve yedek/restore kanıt ekranları görünür | `pnpm --filter @uzman-hocam/web test:e2e`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next login gerçek auth store ile kurum paneline geçer"` |
| Kurum güvenlik denetimi ekranı | Tamamlandı: `/kurum/guvenlik-denetimi` güvenlik audit raporu, production env, canlı RLS, HTTPS smoke, header, auth ve veri kontrol kapılarını gösterir | `pnpm --filter @uzman-hocam/web typecheck`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next login gerçek auth store ile kurum paneline geçer"` |
| Kurum gözlemlenebilirlik ekranı | Tamamlandı: `/kurum/gozlemlenebilirlik` observability UAT, alert webhook, Sentry smoke, dashboard panelleri ve alert kurallarını gösterir | `pnpm --filter @uzman-hocam/web typecheck`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next login gerçek auth store ile kurum paneline geçer"` |
| Kurum UAT/rollback kanıt ekranı | Tamamlandı: `/kurum/uat-rollback` staging/prod UAT raporu, zorunlu smoke komutları, UAT akışları, rollback image tag'i ve restore backup referansını gösterir | `pnpm --filter @uzman-hocam/web typecheck`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next login gerçek auth store ile kurum paneline geçer"` |
| Kurum canlı yayın kanıt ekranı | Tamamlandı: `/kurum/canli-yayin` production evidence zincirini, SMS ve e-posta/push sağlayıcı smoke adımlarını, release özet alanlarını ve dış ortam kanıt gereksinimlerini gösterir | `pnpm --filter @uzman-hocam/web typecheck`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next login gerçek auth store ile kurum paneline geçer"` |
| Kurum sistem sağlığı ekranı | Tamamlandı: `/kurum/sistem-sagligi` API `/health`, `/health/ready` ve `/metrics` sinyallerini kurum admin için özetler; Postgres/Redis hazırlığı ve uptime görünür | `pnpm --filter @uzman-hocam/web typecheck`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next login gerçek auth store ile kurum paneline geçer"` |
| Kurum yedek/restore kanıt ekranı | Tamamlandı: `/kurum/yedek-restore` backup/restore smoke, off-host hedef, WAL arşiv hedefi, restore drill raporu ve kritik tablo kanıtlarını gösterir | `pnpm --filter @uzman-hocam/web typecheck`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next login gerçek auth store ile kurum paneline geçer"` |
| Öğretmen kapsam guard'ı | Tamamlandı: öğretmen öğrenci listesi/veli bağlantısı/yoklama/not/ödev kontrolü, materyal atama, program/etüt okuma ve rapor snapshot listesinde yalnız sorumlu öğrenci veya kendi öğretmen kaydı kapsamını görür | `pnpm --filter @uzman-hocam/api exec vitest run src/tenant/tenant-access.test.ts src/app.e2e.test.ts src/attendance/attendance.e2e.test.ts src/teacher-note/teacher-note.e2e.test.ts src/homework/homework.e2e.test.ts src/program/schedule.service.test.ts src/program/study-session.service.test.ts src/program/schedule.e2e.test.ts src/program/study-session.e2e.test.ts src/report/report-generation.service.test.ts` |
| Ders modeli ve program bağlantısı | Genişledi: `Course` modeli/API'si ve kurum `/kurum/dersler` ekranı eklendi; `TeacherAssignment`, `ScheduleLesson`, `StudySession` ve `HomeworkMaterialAssignment` isteğe bağlı `courseId` ile tenant içinde doğrulanıyor; kurum öğretmen atama ekranı ve öğretmen portalı materyal ataması branşı payload'a yazar | `pnpm --filter @uzman-hocam/api exec vitest run src/school/school.e2e.test.ts src/program/schedule.e2e.test.ts src/program/study-session.e2e.test.ts src/program/schedule-store.test.ts src/program/study-session-store.test.ts`; `pnpm --filter @uzman-hocam/api exec vitest run src/homework/homework-store.test.ts src/homework/homework.e2e.test.ts src/app.e2e.test.ts`; `pnpm --filter @uzman-hocam/web typecheck`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next login gerçek auth store ile kurum paneline geçer"`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next rol portalları bağlı kişi verisini gösterir"` |
| Program ve etüt ekranları | Tamamlandı: kurum `/kurum/program` ve `/kurum/etutler` ekranları sınıf/öğretmen/ders/öğrenci seçimleriyle CRUD yapar; listeler `q/sort/page/limit` alır; öğretmen API okuması kendi ders/etüt kaydıyla sınırlıdır | `pnpm --filter @uzman-hocam/api exec vitest run src/program/schedule.service.test.ts src/program/study-session.service.test.ts src/program/schedule.e2e.test.ts src/program/study-session.e2e.test.ts`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next login gerçek auth store ile kurum paneline geçer"` |
| Akademik yıl/dönem çekirdeği | Tamamlandı: `AcademicYear` ve `AcademicTerm` modeli/API'si, kurum `/kurum/akademik-takvim` ekranı ve program/etüt dönem seçimi eklendi; `TeacherAssignment`, `ScheduleLesson`, `StudySession`, `HomeworkMaterialAssignment` ve `StudentClassHistory` isteğe bağlı `academicYearId/termId` ile tenant içinde doğrulanıyor; kurum öğretmen atama ekranı, öğretmen portalı materyal ataması ve öğrenci sınıf geçmişi dönemi payload'a/ekrana yazar | `pnpm --filter @uzman-hocam/api exec vitest run src/school/school.e2e.test.ts src/program/schedule.e2e.test.ts src/program/study-session.e2e.test.ts`; `pnpm --filter @uzman-hocam/api exec vitest run src/homework/homework-store.test.ts src/homework/homework.e2e.test.ts src/app.e2e.test.ts`; `pnpm --filter @uzman-hocam/api exec vitest run src/student/student-class-history-store.test.ts src/school/school.e2e.test.ts`; `pnpm --filter @uzman-hocam/web typecheck`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next login gerçek auth store ile kurum paneline geçer"`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next rol portalları bağlı kişi verisini gösterir"` |
| Öğrenci kayıt durumu | Genişledi: `Student.status` aktif, pasif, mezun ve nakil durumlarını taşır; mezun/nakil durumuna geçiş aktif `StudentClassHistory` kaydını kapatır; duyuru hedeflemede yalnız aktif öğrenciler alıcı olur; kurum öğrenci ekranı mezun/nakil durumunu seçer ve listeler | `pnpm --filter @uzman-hocam/api exec vitest run src/school/school.e2e.test.ts src/announcement/announcement.e2e.test.ts src/sms-batch/sms-batch.service.test.ts src/sms-batch/sms-batch.e2e.test.ts`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next login gerçek auth store ile kurum paneline geçer"` |
| Öğrenci enrollment/kayıt yenileme | Genişledi: `StudentEnrollment` tenant tablosu ve RLS eklendi; öğrenci oluşturma ilk enrollment kaydını açar, sınıf değişikliği açık enrollmentı kapatıp yenisini açar, mezun/nakil terminal durumu açık enrollmentı kapatır; `/students/:id/enrollments`, `/students/:id/enrollments/renew`, `/students/:id/enrollments/transfer`, `/students/enrollments/bulk-renew`, `/me/student/enrollments`, `/me/student/class-history`, `/me/guardian/students/:studentId/enrollments` ve `/me/guardian/students/:studentId/class-history` API yüzeyi var; kurum öğrenci modalında kayıt yenileme/nakil işlem formu endpointleri çağırır; kurum öğrenci listesinde listelenenleri hedef sınıfa, kaynak sınıf -> hedef sınıf eşlemesine veya otomatik seviye yükseltme kuralına göre geçiren toplu dönem geçişi formu var; hızlı 360, detay 360, öğrenci portalı ve veli portalı kayıt geçmişini gösterir | `pnpm --filter @uzman-hocam/db exec prisma validate --config prisma.config.ts`; `pnpm --filter @uzman-hocam/db run db:rls:check`; `pnpm --filter @uzman-hocam/api exec vitest run src/student/student-enrollment-store.test.ts src/school/school.e2e.test.ts`; `pnpm --filter @uzman-hocam/api exec vitest run src/school/school.e2e.test.ts`; `pnpm --filter @uzman-hocam/web typecheck`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next login gerçek auth store ile kurum paneline geçer"` |
| Öğrenci 360 ilişki bağlamı | Genişledi: kurum öğrenci 360 detayı öğretmen ilişkisinde rolü, sınıfı, dersi ve dönemi okunur adlarla gösterir; sınıf geçmişi akademik yıl/dönem bağlamıyla aynı ekranda kalır | `pnpm --filter @uzman-hocam/web typecheck`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next login gerçek auth store ile kurum paneline geçer"` |
| Kampüs/seviye/şube çekirdeği | Genişledi: `Campus` ve `GradeLevel` modeli/API'si, kurum `/kurum/kampusler` ve `/kurum/seviyeler` ekranları eklendi; `Class` isteğe bağlı `campusId`, `gradeLevelId` ve `section` ile tenant içinde doğrulanıyor; öğrenci/veli profil yüzeyi, sınıf/kayıt geçmişi ve öğretmen profil kapsamı okunur kampüs, seviye ve şube bilgisini gösterir | `pnpm --filter @uzman-hocam/api exec vitest run src/school/school.e2e.test.ts src/student/student-profile.e2e.test.ts`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next rol portalları bağlı kişi verisini gösterir"` |
| Devamsızlık ve öğretmen notu kurum ekranları | Tamamlandı: `Attendance` ve `TeacherNote` isteğe bağlı `courseId/termId` taşır; kurum `/kurum/devamsizlik` ve `/kurum/notlar` ekranları öğrenci/sınıf/ders/dönem bağlamıyla CRUD yapar; listeler `q/sort/page/limit` ve öğrenci sınıfı üzerinden `classId` filtresi alır | `pnpm --filter @uzman-hocam/api exec vitest run src/attendance/attendance.e2e.test.ts src/teacher-note/teacher-note.e2e.test.ts`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next login gerçek auth store ile kurum paneline geçer"` |
| Duyuru hedefleme, okundu ve alıcı raporu | Genişledi: `Announcement` artık `STUDENTS/GUARDIANS` hedef türlerini ve isteğe bağlı `campusId/gradeLevelId/classId/courseId/termId` kapsamını taşır; kurum `/kurum/duyurular` ekranı bu hedeflerle yayın yapar; `/me/student/announcements`, `/me/guardian/students/:studentId/announcements` ve `/me/teacher/announcements` hedefe göre okur; öğrenci, veli ve öğretmen portalında `AnnouncementReceipt` ile okundu bilgisi kalıcı tutulur; kurum admin `/announcements/:id/recipients` ve duyuru ekranından alıcı/okunma raporunu görür | `pnpm --filter @uzman-hocam/api exec vitest run src/announcement/announcement.e2e.test.ts src/app.e2e.test.ts src/me/me-access-matrix.e2e.test.ts`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next login gerçek auth store ile kurum paneline geçer"`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next rol portalları bağlı kişi verisini gösterir"` |
| Öğrenci/veli/öğretmen destek talebi | Genişledi: `SupportTicket.studentId` yanında `campusId/gradeLevelId/classId/courseId/termId` bağlamı eklendi; kurum destek API'si ve `/kurum/destek` ekranı bu alanlarla filtreler; öğrenci `/me/student/support-tickets`, veli izinli bağlı öğrenci için `/me/guardian/students/:studentId/support-tickets`, öğretmen `/me/teacher/support-tickets` yüzeyinden seçili öğrenci/sınıf/ders/dönem bağlamıyla destek talebi açar ve kendi taleplerini listeler; öğretmen kapsam dışı sınıf/öğrenci bağlamıyla talep açamaz; `canOpenSupportTickets=false` veli erişimi 403 olur | `pnpm --filter @uzman-hocam/api exec vitest run src/support-ticket/support-ticket.e2e.test.ts src/support-ticket/support-ticket-store.test.ts src/support-ticket/support-ticket.service.test.ts`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next login gerçek auth store ile kurum paneline geçer"`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next rol portalları bağlı kişi verisini gösterir"` |
| Sınav rapor bağlamı | Genişledi: `ReportSnapshot` artık `campusId/gradeLevelId/classId/courseId/termId` taşır; `POST /exams/:examId/reports/generation-jobs` bağlamı `report-generation` job payload'ına aktarır; worker snapshot kaydına aynı bağlamı yazar ve Postgres sonuç yüklemesini kampüs/seviye/sınıf filtresiyle daraltır; `GET /exams/:examId/reports/snapshots` ve kurum `/kurum/raporlar` ekranı kampüs/seviye/sınıf/ders/dönem filtreleriyle daralır; öğrenci/veli/öğretmen öğrenci raporu cevabı ders/dönem bağlamını taşır ve portal rapor panelinde görünür; kurum rapor ekranı aynı bağlamla rapor üretim kuyruğu başlatır | `pnpm --filter @uzman-hocam/db exec prisma validate --config prisma.config.ts`; `pnpm --filter @uzman-hocam/db run db:rls:check`; `pnpm --filter @uzman-hocam/api exec vitest run src/report/report-generation.service.test.ts src/report/report-generation.controller.e2e.test.ts src/report/report-snapshot-store.test.ts src/queue/job-producer.test.ts`; `pnpm --filter @uzman-hocam/api exec vitest run src/school/school.e2e.test.ts src/app.e2e.test.ts`; `pnpm --filter @uzman-hocam/worker exec vitest run src/jobs/report-generation-job.test.ts src/jobs/postgres-report-generation-adapter.test.ts`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next login gerçek auth store ile kurum paneline geçer"`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next rol portalları bağlı kişi verisini gösterir"` |
| Veli bildirim tercihleri | Tamamlandı: veli yalnız bağlı öğrencisi için `/me/guardian/students/:studentId/notification-preferences` yüzeyinden `canReceiveSms`, `canReceiveAnnouncements` ve `canOpenSupportTickets` tercihlerini görür/günceller; bağlı olmayan öğrenci 403 olur | `pnpm --filter @uzman-hocam/api exec vitest run src/support-ticket/support-ticket.e2e.test.ts src/me/me-access-matrix.e2e.test.ts`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next rol portalları bağlı kişi verisini gösterir"` |
| SMS batch teslim raporu | Kısmi tamamlandı: `SmsBatchDeliveryReport` tenant tablosu ve RLS eklendi; `POST /sms-batches` aynı `jobId` ile queued rapor kaydı oluşturur; kurum admin `GET /sms-batches/:jobId` ile raporu okur; worker SMS sonucunu aynı kayda `completed/failed`, gönderilen/başarısız adet ve segment toplamıyla yazar; `POST /sms-batches/recipients/preview` duyuru hedefinden veya sınıf/kampüs/seviye/ders/dönem/öğrenci durumu filtresinden `canReceiveSms=true` ve telefonu olan velileri üretir; kurum `/kurum/sablonlar` ekranı duyuru hedefi veya manuel filtreyle SMS gönderir, mesaj önizlemesi ve teslim raporunu gösterir; kurum `/kurum/duyurular` ekranı veli/öğrenci/tüm okul duyurusundan seçili SMS şablonuyla batch başlatır ve teslim raporunu gösterir; öğretmen erişimi 403 olur | `pnpm --filter @uzman-hocam/db run db:rls:check`; `pnpm --filter @uzman-hocam/api exec vitest run src/sms-batch/sms-batch.service.test.ts src/sms-batch/sms-batch.e2e.test.ts`; `pnpm --filter @uzman-hocam/worker exec vitest run src/jobs/sms-batch-job.test.ts src/jobs/sms-batch-processor.test.ts src/jobs/postgres-sms-batch-delivery-reporter.test.ts`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next login gerçek auth store ile kurum paneline geçer"` |
| Duyuru dış bildirim teslim raporu | Genişledi: `AnnouncementDeliveryReport` tenant tablosu ve RLS eklendi; EMAIL/PUSH için duyuru bazlı alıcı, teslim, hata ve durum özeti tutulur; kurum admin `GET /announcements/:id/delivery-reports` ile okur, öğretmen erişimi 403 olur; kurum admin `POST /announcements/:id/delivery-results` ile sağlayıcı teslim sonucunu `announcement-delivery` kuyruğuna bağlar; worker processor sonucu aynı rapora upsert eder; `@uzman-hocam/notification-adapter` EMAIL/PUSH için no-op prod engeli ve HTTP sağlayıcı sözleşmesi sunar; kurum admin `POST /announcements/:id/deliveries` ile EMAIL/PUSH gönderimini gerçek duyuru alıcılarından üretip adapter sonucunu rapor kuyruğuna bağlar; `/me/notification-devices` kullanıcı cihaz tokenlarını tenant içinde kaydeder/listeler/kapatır; Next app shell `Bildirim cihazı` paneli Web Push aboneliğini alıp cihaza kaydeder | `pnpm --filter @uzman-hocam/db exec prisma validate --config prisma.config.ts`; `pnpm --filter @uzman-hocam/db run db:rls:check`; `pnpm --filter @uzman-hocam/api exec vitest run src/announcement/announcement-delivery-report-store.test.ts src/announcement/announcement.e2e.test.ts`; `pnpm --filter @uzman-hocam/api exec vitest run src/me/me-access-matrix.e2e.test.ts src/announcement/announcement.e2e.test.ts`; `pnpm --filter @uzman-hocam/web typecheck`; `pnpm --filter @uzman-hocam/web exec playwright test -c playwright.next.config.ts e2e-next/login-next.spec.ts -g "Next rol portalları bağlı kişi verisini gösterir"`; `pnpm --filter @uzman-hocam/api exec vitest run src/queue/job-producer.test.ts src/queue/bullmq-producer.test.ts`; `pnpm --filter @uzman-hocam/worker exec vitest run src/jobs/announcement-delivery-job.test.ts src/jobs/announcement-delivery-processor.test.ts src/jobs/postgres-announcement-delivery-reporter.test.ts src/queue/bullmq-worker.test.ts`; `pnpm --filter @uzman-hocam/notification-adapter test` |
| Geniş negatif erişim matrisi | Tamamlandı: öğrenci/veli/öğretmen `/me` yüzeyleri için yanlış rol, hiyerarşi tuzağı, bağlı olmayan öğrenci ve başka tenant denemeleri tek e2e matrisinde doğrulanıyor | `pnpm --filter @uzman-hocam/api exec vitest run src/me/me-access-matrix.e2e.test.ts src/app.e2e.test.ts src/student/student-profile.e2e.test.ts` |

## Sonraki Uygulama Sırası

1. Kimlik-bağı davet/göç akışı için ürün onayı.
2. Gerçek e-posta/push credential smoke'u.
3. Netgsm test credential/canlı hesap doğrulaması ve `pnpm sms:smoke` staging sonucu.
